<?xml version="1.0"?>
<openerp>
    <data>

        <!-- Add salesinq tab and external_id to product.product -->
        <record id="product_normal_form_view_xml_id" model="ir.ui.view">
            <field name="name">view.product.product.form.xml_id</field>
            <field name="model">product.product</field>
            <field name="inherit_id" ref="product.product_normal_form_view"/>
            <field eval="7" name="priority"/>
            <field name="arch" type="xml">
                <field name="default_code" position="after">
                    <field name="is_salesinq_able" invisible="1"/>
                </field>
                <page string="Sales" position="after">
                    <page string="SalesInq" attrs="{'invisible':[('is_salesinq_able','!=',True)]}">
                        <field name="salesinq_data" nolabel="1"/>
                    </page>
                </page>
            </field>
        </record>

    </data>
</openerp>
